# Render Environment Template
# Copy these values to your Render service environment variables

APP_NAME="Rainlo API"
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:2sz808BitecfFDChCP410Yu4nCOb62tnUDPzBPEyjSc=
APP_URL=https://rainlo-api.onrender.com

# Database (automatically configured by Render)
DB_CONNECTION=pgsql
# DB_HOST, DB_PORT, DB_DATABASE, DB_USERNAME, DB_PASSWORD 
# will be automatically set by Render from the database service

# Cache and Session
CACHE_STORE=file
SESSION_DRIVER=file
QUEUE_CONNECTION=database

# Logging
LOG_CHANNEL=stderr
LOG_LEVEL=info

# Security
BCRYPT_ROUNDS=12

# Mail Configuration (optional - update with your SMTP settings)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@rainlo.app
MAIL_FROM_NAME="Rainlo"
